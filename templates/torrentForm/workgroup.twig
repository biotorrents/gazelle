{# workgroup #}
<tr>
	<th>
		<label for="workgroup" class="required">{{ db.name }}</label>
	</th>

	<td>
		<input type="text" id="workgroup" name="workgroup" maxlength="128" placeholder="{{ db.description }}" value="{{ value }}" required>
	</td>
</tr>
