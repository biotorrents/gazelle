{# version #}
<tr id="audio_tr">
	<th>
		<label for="version">{{ db.name }}</label>
	</th>

	<td>
		<input type="text" id="version" name="version" pattern="\d+\.*\d*\.*\d*" placeholder="{{ db.desc }}" value="{{ value }}"/>

		<aside>{{ db.note|raw }}</aside>
	</td>
</tr>
