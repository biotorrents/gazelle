{# identifier #}
<tr>
	<th>
		<label for="identifier">
			{{ db.name }}
		</label>
	</th>

	<td>
		<input type="text" id="identifier" name="identifier" maxlength="64" placeholder="{{ db.desc }}" value="{{ value }}"/>
	</td>
</tr>
