{# year #}
<tr>
	<th>
		<label for="year" class="required">{{ db.name }}</label>
	</th>

	<td>
		<input type="text" id="year" name="year" maxlength="4" placeholder="{{ db.description }}" value="{{ value }}" required>
	</td>
</tr>
