{##
 # breadcrumbs
 #}

<aside class="breadcrumbs">
    <ul>
        <li class="faded"><a href="/">home</a></li>
        {% for key, value in breadcrumbs %}
            <li class="faded"><a href="{{ key }}">{{ words(value, 5)|lower }}</a></li>
        {% endfor %}
    </ul>
</aside>
