{% extends "base.twig" %}


{% block content %}

<h2>Admin tools</h2>

<p>Various tools to help manage the site.</p>


<div id="adminGrid">


{# administration #}
<section>
    <h3>Administration</h3>

    <ul>
        {% if can({"toolbox": "clientWhitelist"}) %}
            <li><a href="/toolbox/client-whitelist">Client whitelist</a></li>
        {% endif %}

        {% if can({"toolbox": "permissionsManager"}) %}
            <li><a href="/toolbox/permissions-manager">Permissions manager</a></li>
        {% endif %}

        {% if can({"toolbox": "databaseKey"}) %} 
            <li><a href="/toolbox/database-key">Database key</a></li>
        {% endif %}
    </ul>
</section>

{# announcements #}
<section>
    <h3>Announcements</h3>

    <ul>
        {% if can({"toolbox": "globalNotifications"}) %}
            <li><a href="/toolbox/global-notifications">Global notifications</a></li>
        {% endif %}

        {% if can({"toolbox": "massPm"}) %}
            <li><a href="/toolbox/mass-pm">Mass PM</a></li>
        {% endif %}

        {% if can({"toolbox": "newsPosts"}) %} 
            <li><a href="/toolbox/news-posts">News posts</a></li>
        {% endif %}
    </ul>
</section>

{# community #}
{#
<section>
    <h3>Community</h3>

    <ul>
        {% if can({"toolbox": "forumManager"}) %}
            <li><a href="/toolbox/forum-manager">Forum manager</a></li>
        {% endif %}
   </ul>
</section>
#}

{# queue #}
<section>
    <h3>Queue</h3>

    <ul>
        {% if can({"toolbox": "autoEnableRequests"}) %}
            <li><a href="/toolbox/auto-enable-requests">Auto-enable requests</a></li>
        {% endif %}

        {% if can({"toolbox": "loginWatch"}) %}
            <li><a href="/toolbox/login-watch">Login watch</a></li>
        {% endif %}
    </ul>
</section>

{# managers #}
<section>
    <h3>Managers</h3>

    <ul>
        {% if can({"toolbox": "emailBlacklist"}) %}
            <li><a href="/toolbox/email-blacklist">Email blacklist</a></li>
        {% endif %}

        {% if can({"toolbox": "ipAddressBans"}) %}
            <li><a href="/toolbox/ip-address-bans">IP address bans</a></li>
        {% endif %}

        {% if can({"toolbox": "manipulateInviteTree"}) %} 
            <li><a href="/toolbox/manipulate-invite-tree">Manipulate invite tree</a></li>
        {% endif %}
    </ul>
</section>

{# development #}
<section>
    <h3>Development</h3>

    <ul>
        {% if can({"toolbox": "serviceStats"}) %}
            <li><a href="/toolbox/service-stats">Service stats</a></li>
        {% endif %}

        {% if can({"toolbox": "miscellaneousValues"}) %} 
            <li><a href="/toolbox/miscellaneous-values">Miscellaneous values</a></li>
        {% endif %}

        {% if can({"toolbox": "trackerInformation"}) %} 
            <li><a href="/toolbox/tracker-information">Tracker information</a></li>
        {% endif %}
    </ul>
</section>

{# site information #}
<section>
    <h3>Site information</h3>

    <ul>
        {% if can({"toolbox": "invitePool"}) %}
            <li><a href="/toolbox/invite-pool">Invite pool</a></li>
        {% endif %}

        {% if can({"toolbox": "registrationLog"}) %}
            <li><a href="/toolbox/registration-log">Registration log</a></li>
        {% endif %}

        {% if can({"toolbox": "upscalePool"}) %} 
            <li><a href="/toolbox/upscale-pool">Upscale pool</a></li>
        {% endif %}
    </ul>
</section>

{# torrents #}
<section>
    <h3>Torrents</h3>

    <ul>
        {% if can({"toolbox": "collageRecovery"}) %}
            <li><a href="/toolbox/collage-recovery">Collage recovery</a></li>
        {% endif %}

        {% if can({"toolbox": "freeleechTokenManager"}) %}
            <li><a href="/toolbox/freeleech-token-manager">Freeleech token manager</a></li>
        {% endif %}

        {% if can({"toolbox": "multipleFreeleech"}) %} 
            <li><a href="/toolbox/multiple-freeleech">Multiple freeleech</a></li>
        {% endif %}

        {% if can({"toolbox": "tagAliases"}) %} 
            <li><a href="/toolbox/tag-aliases">Tag aliases</a></li>
        {% endif %}

        {% if can({"toolbox": "batchTagEditor"}) %} 
            <li><a href="/toolbox/batch-tag-editor">Batch tag editor</a></li>
        {% endif %}

        {% if can({"toolbox": "officialTagsManager"}) %} 
            <li><a href="/toolbox/official-tags-manager">Official tags manager</a></li>
        {% endif %}

        {% if can({"toolbox": "sitewideFreeleechManager"}) %} 
            <li><a href="/toolbox/sitewide-freeleech-manager">Sitewide freeleech manager</a></li>
        {% endif %}
    </ul>
</section>


</div>


{% endblock %}


{% block sidebar %}

	{% include "admin/sidebar.twig" %}

{% endblock %}