{% extends "base.twig" %}

{% block content %}

	<section>
		<h2>{{ subject }}</h2>

		<p>{{ body }}</p>

        {#
		<p>
			<a href="/search/log/{{ subject }}">Search the site log</a>
		</p>
        #}
	</section>

{% endblock %}
